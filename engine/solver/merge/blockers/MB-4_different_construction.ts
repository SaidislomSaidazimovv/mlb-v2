// MB-4 — Разная конструкция
//
// САМЫЙ ВАЖНЫЙ ЗАПРЕТ. Тип шкафа выбирает scope профиля (byType), а scope задаёт паз задней стенки, накладное/вкладное дно, цоколь. Общий бок принадлежит обоим шкафам сразу — значит, он должен подчиняться ДВУМ конструкциям одновременно. Это прямое нарушение DB/27, и с уже запущенной пилой.

import type { MergeBlocker } from "../types.js";

export const MB_4: MergeBlocker = {
  id: "MB-4",
  title: "Разная конструкция",
  why: "САМЫЙ ВАЖНЫЙ ЗАПРЕТ. Тип шкафа выбирает scope профиля (byType), а scope задаёт паз задней стенки, накладное/вкладное дно, цоколь. Общий бок принадлежит обоим шкафам сразу — значит, он должен подчиняться ДВУМ конструкциям одновременно. Это прямое нарушение DB/27, и с уже запущенной пилой.",
  source: "DB/27 §4 — единственный источник конструкции",
  blocks(c) {
    const a = c.left.cabinetType, b = c.right.cabinetType;
    if (a !== b) return `разные типы (${a ?? "—"} и ${b ?? "—"}): общий бок подчинялся бы двум конструкциям сразу (DB/27)`;
    return null;
  },
};
