// MB-8 — Слишком много шкафов в одном корпусе
//
// ЕДИНСТВЕННЫЙ запрет-НАСТРОЙКА в этом списке. 5 секций одним корпусом могут пройти и по листу, и по весу — и всё равно оказаться тем, что не занести по лестнице и не развернуть в коридоре. Это мнение цеха, а не физика.

import type { MergeBlocker } from "../types.js";

export const MB_8: MergeBlocker = {
  id: "MB-8",
  title: "Слишком много шкафов в одном корпусе",
  why: "ЕДИНСТВЕННЫЙ запрет-НАСТРОЙКА в этом списке. 5 секций одним корпусом могут пройти и по листу, и по весу — и всё равно оказаться тем, что не занести по лестнице и не развернуть в коридоре. Это мнение цеха, а не физика.",
  source: "Основатель 2026-08-15 · merge.limits.maxCabinetsPerCarcass",
  blocks(c) {
    const max = c.profile.defaults.merge.limits.maxCabinetsPerCarcass;
    if (c.groupSoFar.length + 1 > max) return `в одном корпусе было бы ${c.groupSoFar.length + 1} шкафов, предел ${max}`;
    return null;
  },
};
